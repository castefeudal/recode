# Android Health Connect bridge

Build this module with the official Godot Android plugin template and Android Studio. The bridge requests only declared read permissions, reports denial without blocking manual mode, and contains no credentials. See `docs/HEALTH_INTEGRATIONS.md`.
