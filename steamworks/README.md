# Steamworks

Replace IDs only in local release configuration. Upload preview first, validate depots, then use a private beta branch. Configure achievements from `game/data/achievements.json`, Rich Presence, Cloud Save paths and controller layout.
